spring:
  application:
    name: eureka-producer
  cloud:
    config:
      uri: http://localhost:1026/
      profile: dev
      label: master

management:
  endpoints:
    web:
      exposure:
        include: refresh
